# Frisco Fencing Academy — History

One row per shipped feature/PR, most recent first.

| Date | Change |
|---|---|
| 2026-08-21 | **CKQ parity Phase 4 (of 4) — Private class flow, on `feature/ckq-parity`, not yet merged.** Coach contracts (`CoachContract`, one active per coach) → coaches (or admin on their behalf) publish weekly `PrivateClassSchedule` slots → unauthenticated public browse (`GET /private-class-schedules/public`, server-computed price + first-session date, no student/parent leak) → parent self-registration (`privateClassEnrollment.service.js create()`, born active, rate pinned from the contract, atomic `findOneAndUpdate` slot claim) → 8-week `PrivateClassSession` generation → coach marks attendance → `attended` triggers `chargeSession()`'s three-layer-idempotent per-session Stripe `PaymentIntent` (pre-check → unique-partial-index E11000 dedup → suffixed idempotency key `pcs_${sessionId}_${attempt}`) → cancellation frees slots + deletes only future sessions. Four `CKQ-BUG-FIX`es vs. the CKQ reference: atomic slot claim (was a read-then-write race), coach-ownership check on attendance (was any-coach-marks-any-session), attempt-suffixed Stripe idempotency key (was a 24h cached-decline replay bug blocking same-day retry), and a payment-failed email (CKQ sends nothing on decline). New models: `CoachContract`, `PrivateClassSchedule`, `PrivateClassEnrollment`, `PrivateClassSession`, `PrivateClassCharge`. New pages: public `/private-classes`, parent `/parent/register-private` (3-step wizard) + a Private Lessons section on `/parent/subscriptions`, coach `/coach/private-students` (attendance + retry-charge), admin `/admin/coach-contracts` + `/admin/private-classes`. Backend: 251/251 tests across 28 suites (incl. real Stripe TEST-mode charge/decline/retry coverage and the mandatory cancel-then-charge race + idempotency regressions). Frontend: 181/181 tests across 37 suites. `tsc --noEmit` 0 errors, `next build` clean. Plan: `docs/plans/ckq-parity-plan.md` §5; feature doc: `docs/features/private-class.md`. |
| 2026-08-21 | **CKQ parity Phase 3 (of 4) — Admin Group Class Subscriptions page, on `feature/ckq-parity`.** New `GET/PATCH /api/v1/subscriptions*` surface: `listAll` (status incl. the `pending_cancel` split, `q` search, pagination), `reactivate` (reverses a pending cancellation, mirrors `cancel()`'s permission shape), `changeSchedule` (admin-only, same-level-only per D6 — always price-neutral, no delta charge/proration/sibling-discount recompute — validates level match/capacity/duplicate-subscription, then moves the `Subscription`/`Registration` pointers and both schedules' rosters+future sessions). Extracted the roster add/remove logic — previously duplicated in `registration.service.js` and `renewal.service.js` — into a shared `roster.service.js`, reused by all three call sites. `cancel()` now sends the cancellation confirmation email (previously sent nothing). New `/admin/subscriptions` page (search+status filter, status chips, 2-step Change Schedule dialog filtered to the same level, Cancel/Reactivate confirm dialogs), new Billing nav section. Plan: `docs/plans/ckq-parity-plan.md` §4; feature doc: `docs/features/admin.md`. |
| 2026-08-21 | **CKQ parity Phase 2 (of 4) — Block-based email design system + all templates, on `feature/ckq-parity`.** Ported CKQ's three-layer email architecture (`templates.js` block registry → `layout.js` HTML renderer → `text.js` plain-text twin derived from the same blocks, so HTML and text can never drift) into `backend/src/email/`, rebranded to Frisco tokens (frozen hex palette, `ffa-` class prefix, text-wordmark header fallback when `LOGO_URL` is unset). 10 templates (V2-only from day one — no legacy fallback, Frisco is greenfield): the 3 pre-existing sends rewired plus 7 new (cancellation/reactivation/schedule-change confirmations, and the 4 private-class templates used by Phase 4). `mail.service.js` rewired to render via `renderEmail()` and added CC support (filtered of falsy entries). New `backend/scripts/preview-emails.js` (renders every template to `backend/email-preview/*.html`/`*.txt`, gitignored) — the QA loop for a hard-blocked staging environment. Plan: `docs/plans/ckq-parity-plan.md` §3; module doc: `docs/modules/email.md`. |
| 2026-08-21 | **CKQ parity Phase 1 (of 4) — Fail-closed staging email gate, on `feature/ckq-parity`.** New `APP_ENV` env var (fail-closed: anything other than `production` blocks real SMTP sends from `mail.service.js`'s `sendMailSafely`) — closes the gap where staging (`friscofencing-staging` DB, Preview deploys) could email real parents through the same Brevo SMTP creds as production. Checked *after* the message is fully rendered, so staging still exercises the full render path; only the final `transporter.sendMail` call is skipped. Ethereal (no `SMTP_HOST`, the zero-setup local-dev path) is exempt — it never delivers to a real inbox. `NODE_ENV` deliberately not used for the gate — Vercel sets `production` on Preview deployments too. New env vars `APP_ENV`/`ADMIN_EMAIL`/`LOGO_URL` documented in `backend/.env.example` and `docs/plans/deployment-launch-plan.md`'s §4a table + deferred-follow-ups. Plan: `docs/plans/ckq-parity-plan.md` §2. |
| 2026-08-21 | **Production promotion (PR #13, main)**: the entire CKQ UI adoption plan (7 phases — admin sidebar shell, admin CRUD edit/delete, parent portal shell + dashboard, registration flow wizards, child detail page, testing/docs reorg), the parent-logout regression fix (PR #11), and admin user management (PR #12) all went live in production in one merge. Hit the recurring serverless cold-start MongoDB bug immediately after (4th occurrence this launch) — fixed with a redeploy; flagged as the top deferred item in `deployment-launch-plan.md` for a proper fix. |
| 2026-08-21 | **Admin Users management — create, edit, change password, delete.** Backend: `user.service.js` (new) adds `create`/`update`/`updatePassword`/`remove` alongside the existing `list`, wired through new `POST /users`, `PUT /users/:id`, `PUT /users/:id/password`, `DELETE /users/:id` routes (admin/superadmin only). Role is immutable after creation; only a superadmin may create/edit/password-reset/delete a superadmin account (403 otherwise, and `list` hides superadmin rows from a non-superadmin's unfiltered/all-role query) — deliberately stricter than the CKQ reference this was adapted from. Self-delete blocked (400). Delete guards: parent blocked by existing children, student blocked by `Registration`/`Subscription`/`TrialClass`, coach blocked by `GroupClassSchedule`; admin/superadmin have no entity guard. Frontend: new `/admin/users` page (Pattern A + role tabs + a third Change Password dialog), `lib/services/users.ts`, new "People" nav section, dashboard quick-link. Plan: `docs/plans/admin-user-management-plan.md`. |
| 2026-08-20 | **CKQ UI adoption Phase 6 — Testing & docs organization (plan complete, all 7 phases shipped to develop).** `docs/TESTING_STRATEGY.md` expanded (layers, mocking rules incl. named exceptions, interaction rule for new tests, date rules, typed fixtures, naming/isolation conventions, what-NOT-to-test, error-handling contract). New `docs/TEST_COVERAGE.md` (real re-run numbers: backend 19 suites/99 tests, frontend 30 suites/137 tests, both under `TZ=UTC`; honest gaps + improvement plan). New `docs/decisions/README.md` + ADR `002-ckq-ui-adoption.md`. `docs/design-system.md` consolidated to Principles → Tokens → Shells → Page patterns → Components inventory → Anti-patterns → Pre-merge checklist. `CLAUDE.md` documentation map updated, plan marked SHIPPED, new pre-read-requirements table added. Plan: `docs/plans/ckq-ui-adoption-plan.md`, Phase 6 of 7 (final). |
| 2026-08-20 | **CKQ UI adoption Phase 5 — Child detail page + AddChildModal.** New `/parent/child/[id]` page (reads from `ParentPortalContext`, no new fetch; `?tab=` URL param for Overview/Schedule, validated against a fixed Set with fallback; not-found state). New `AddChildModal` extracted from the inline add-child form, used by the children page, the sidebar's "+ Add child" row, and the dashboard's empty-state CTA. Sidebar child rows and dashboard child cards now link to `/parent/child/[id]`. Plan: `docs/plans/ckq-ui-adoption-plan.md`, Phase 5 of 7. |
| 2026-08-20 | **CKQ UI adoption Phase 4 — Registration flow wizards.** New flow kit (`FlowMain`/`FlowStepper`, `FlowSection`, `ChildPickerCards`, `OrderSummary` — owns the single per-step CTA, `FlowConfirmation`). Book a Trial rebuilt as a 3-step wizard (Who → Pick a Class → Confirmation), Register as a 4-step wizard (Who → Class → Review & Pay → Done, saved-payment-method guard moved to Review). Both support a `?child=` deep-link preselect and preserve selections on back-navigation. **Payment-critical guarantee held**: both wizards call the identical Phase-0 `bookTrialClass`/`createRegistration` mutation services with byte-identical payloads — only the UI changed; existing payload-assertion tests adapted, not weakened. Payment-method page left as-is (already portal-card-styled from Phase 3; no further restyle to avoid touching Stripe `CardElement` logic). Plan: `docs/plans/ckq-ui-adoption-plan.md`, Phase 4 of 7. |
| 2026-08-20 | **CKQ UI adoption Phase 3 — Parent portal shell, context, dashboard.** New `PortalLayout` (light sidebar primitive, longest-href-prefix active match, mobile bottom tab bar) + `ParentPortalShell` (Home/Children/Academy nav groups, per-child rows with deterministic palette avatars + status line, header greeting + children-count chip). New `ParentPortalContext` (`Promise.allSettled` over students/subscriptions/trialClasses — error only on the primary students fetch, empty household is not an error). `app/parent/layout.tsx` role-gates parent-only and wraps every parent page. New `/parent/dashboard` (loading/empty-onboarding/populated states, data from context only). Children page now consumes context instead of its own fetch. All 5 parent pages unwrapped from `ProtectedRoute`/`AppShell`; `AppShell` no longer serves parent. New `lib/childPalette.ts`, `docs/features/parent-portal.md`. Plan: `docs/plans/ckq-ui-adoption-plan.md`, Phase 3 of 7. |
| 2026-08-20 | **CKQ UI adoption Phase 2 — Admin CRUD rebuild (edit/delete delivered).** Backend: added missing in-use delete guards — `GroupClass.remove()` now 409s if a `GroupClassSchedule` references it; `Level.remove()` now also 409s if a `Price` is configured for it (previously only checked `GroupClass`). Frontend: Locations/Levels/Prices/Classes rebuilt onto Pattern A (one dialog for create+edit, small delete-confirm dialog that flips to "Cannot Delete" on a 409, optimistic row removal). Schedules restyled + create-form modal-ified, no edit/delete (deferred — ripple effects on generated sessions/rosters). Sessions page restyled. Attendance page untouched. New `docs/features/admin.md`. Plan: `docs/plans/ckq-ui-adoption-plan.md`, Phase 2 of 7. |
| 2026-08-20 | **CKQ UI adoption Phase 1 — Admin sidebar shell.** `app/admin/layout.tsx` — dark sidebar (dashboard + Programs/Schedule/Places sections, auto-open on active route, mobile drawer), role-gated (admin/superadmin only, redirects everyone else). `admin.module.css` design system (buttons, table, dialog, form, chips, stat cards) + `AdminPageHeader`/`AdminTableRows` primitives. New `/admin` (redirect) and `/admin/dashboard` (raw list counts + quick links) pages. All 6 admin pages unwrapped from `ProtectedRoute`/`AppShell`. `AppShell` no longer serves admin/superadmin. Plan: `docs/plans/ckq-ui-adoption-plan.md`, Phase 1 of 7. |
| 2026-08-20 | **CKQ UI adoption Phase 0 — Foundations.** `lucide-react` added; fixed the 66 `tsc` jest-dom-matcher errors (`types/jest-dom.d.ts`); `lib/types.ts` (domain interfaces typed against real backend responses); `lib/services/{catalog,scheduling,parent}.ts` (queries throw, mutations return `{status:'success'\|'error'}`, never throw); `lib/hooks/useLoadState.ts` + `getErrorMessage`; `app/components/ui/LoadError/`; shell CSS tokens in `globals.css`. No page rewired yet — pure foundation for Phases 1–5. Plan: `docs/plans/ckq-ui-adoption-plan.md`. |
| 2026-08-20 | **Live in production**: `friscofencing.vercel.app` (frontend) + `friscofencing-backend.vercel.app` (backend), both verified end-to-end (home page, superadmin login, Secure/HttpOnly cookie, Atlas connectivity). Two Vercel projects wired to GitHub `main`/`develop` → Atlas `friscofencing`/`friscofencing-staging`. |
| 2026-08-20 | Vercel deploy readiness: serverless entry (`backend/api/index.js` + `vercel.json`), cookie `secure` flag by NODE_ENV, frontend `/api/v1` rewrite proxy (first-party auth cookie on Vercel), relative axios baseURL. GitHub repo + develop/main branches created; Atlas cluster with `friscofencing` + `friscofencing-staging` DBs seeded. Plan: `docs/plans/deployment-launch-plan.md`. Known gap found in owner testing: admin pages have no edit/delete UI (backend routes exist) — next feature. |
| 2026-08-08 | Public home page (hero, offerings, CTA, footer) + login/register cross-links + a "book a free trial" prompt on /parent/children. Registration deliberately stays immediate-access, no email verification (CKQ's real flow hard-gates on it; deferred here by choice). |
| 2026-08-07 | Design system applied across all 17+ pages (auth, admin, parent, coach, shared attendance). Two real bugs found during the Phase 12 live E2E pass fixed along the way: same-day trial sessions were always excluded from booking (date-vs-now comparison bug in book-trial), and cancelling a subscription showed "undefined undefined-undefined" until reload (unpopulated cancel response merged into state instead of refetching). Both have regression tests. |
| 2026-08-07 | Design system foundation: tokens (near-black primary, gold accent-only), Saira/Saira Condensed fonts, Button/Alert/Card, AppShell (role-aware nav). Brand inspired by FencerIQ, rebalanced for a dense CRM. See `docs/design-system.md`. |
| 2026-08-06 | Phase 11: Stripe webhook (scoped: record + dedup payment_intent.succeeded/failed, no reconciliation). Raw-body middleware ordering verified correct. |
| 2026-08-06 | Phase 10: confirmation/receipt emails (trial, registration, renewal) via mail.service.js -- never throws, zero-setup local dev via Ethereal auto-fallback when SMTP_HOST is unset. |
| 2026-08-06 | Phase 9: idempotent renewal job (renewOne/runRenewals) + two-stage cancellation. Implements ADR 001 in full. No scheduler yet -- standalone script (`npm run renewals`). |
| 2026-08-06 | Phase 8: 10% sibling discount in calculateChargeAmount -- dynamic lower-payer rule, deterministic tie-break, live re-derivation every call. Known accepted limitation: simultaneous-first-registration race, not fixed (real usage is serial). |
| 2026-08-06 | Phase 7b: Registration + Subscription -- first-payment charge on enrollment (real Stripe test-mode PaymentIntent), roster + future-session backfill, /parent/register page. Completes Phase 7. |
| 2026-08-06 | Phase 7a: Stripe Customer + saved-card flow (card save only, no charging yet). Backend integration test hits real Stripe test-mode API. |
| 2026-08-06 | Trial class booking — public parent signup, student creation (parentId forced server-side), TrialClass (one ever, adds student to session roster), /register + /parent/children + /parent/book-trial pages. |
| 2026-08-06 | Attendance marking — PATCH endpoint (admin: any session; coach: own assigned schedule only, checked in the service), shared attendance page for coach + admin, new coach schedule/session listing pages. |
| 2026-08-06 | Price model + admin CRUD — one monthlyFee per level, looked up dynamically at billing time (no FK on GroupClass). |
| 2026-08-06 | Location/Level/GroupClass/Schedule/Session models + CRUD + admin pages. Schedule creation auto-generates an 8-week session window (no cron). `ProtectedRoute` frontend guard added. |
| 2026-08-06 | Users/Auth: `User` model, JWT/Passport (httpOnly `accessToken` cookie), login/me/logout endpoints, idempotent superadmin seed script, frontend `AuthContext` + login page. Integration tests use `mongodb-memory-server`; frontend auth test uses MSW. |
| 2026-08-06 | Project scaffolded — repo structure, backend/frontend skeletons, docs (CLAUDE.md, TESTING_STRATEGY.md, DATABASE_SCHEMA_DOCUMENTATION.md, ADR 001). |
